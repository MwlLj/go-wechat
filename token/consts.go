package token

var (
	GrantType string = "grant_type"
	AppId     string = "appid"
	AppSecret string = "secret"
)

var (
	ClientCredential string = "client_credential"
)

var (
	GetTokenUrl string = "https://api.weixin.qq.com/cgi-bin/token"
)
